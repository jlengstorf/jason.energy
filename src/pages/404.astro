---
import Layout from '../layouts/default.astro';
import Header from '../components/header.astro';
import Footer from '../components/footer.astro';
---

<Layout title="404 — Page Note Found" simple={true}>
	<Header />

	<section>
		<h1>Page Not Found</h1>

		<p>
			The page you requested was not found. If you got here from a link or
			otherwise think this is in error, please <a
				href="mailto:jason@lengstorf.com?subject=Broken+link+on+jason.energy"
				>send an email to let me know</a
			>.
		</p>
	</section>

	<Footer />
</Layout>

<style>
	section {
		padding: 80px 5dvi;
	}

	h1 {
		--font-size: 1;

		color: var(--white);
		font-family: var(--font-heading);
		font-size: calc(var(--font-size) * clamp(1rem, 2.5cqi, 4rem));
		line-height: 1.1;
		margin: 0;
		padding: 0;
		text-align: center;
	}

	p {
		margin: 16px auto 0;
		max-inline-size: 54ch;
		text-align: center;
		text-wrap: balance;
	}
</style>
