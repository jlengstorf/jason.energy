---
export interface Props {
	id: string;
}

const { id } = Astro.props;
---

<aside class="fntext" id={id}><slot /></aside>

<style>
	aside {
		background: var(--gray-600);
		block-size: 0;
		border-radius: 3px;
		overflow-y: clip;
		padding: 0 32px;
		transition-property: block-size, padding;
		transition-duration: 250ms;
		transition-timing-function: linear;

		@starting-style {
			block-size: 0;
			padding: 0 32px;
		}

		&.visible {
			block-size: calc-size(max-content, size);
			display: block;
			padding: 16px 32px;
		}

		> * {
			margin: 0;

			&:not(:first-child) {
				margin-top: 16px;
			}
		}
	}
</style>
